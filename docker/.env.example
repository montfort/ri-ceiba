# Database Configuration
DB_NAME=ceiba
DB_USER=ceiba
DB_PASSWORD=change_me_in_production

# SSL Certificate
CERT_PASSWORD=change_me_in_production

# SMTP Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@ceiba.ssc.cdmx.gob.mx
SMTP_PASSWORD=change_me_in_production
EMAIL_FROM=noreply@ceiba.ssc.cdmx.gob.mx

# AI Configuration (OpenAI, AzureOpenAI, or Local)
AI_PROVIDER=OpenAI
AI_API_KEY=change_me_in_production
AI_MODEL=gpt-4

# Automated Reports
REPORT_TIME=06:00:00
REPORT_RECIPIENTS=["admin@ceiba.ssc.cdmx.gob.mx","supervisor@ceiba.ssc.cdmx.gob.mx"]

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Update all passwords and API keys
# 3. Never commit .env to version control
